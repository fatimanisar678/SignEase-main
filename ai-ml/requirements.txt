# SignEase ML API — Python dependencies
# -------------------------------------------------------
# IMPORTANT: TensorFlow is NOT required (removed).
# The app uses the sklearn RandomForest model (model.p).
#
# Install with:
#   pip install -r requirements.txt
#
# Python 3.9–3.12 recommended.

flask>=2.3.0
flask-cors>=4.0.0
mediapipe>=0.10.9
opencv-python-headless>=4.8.0
numpy>=1.24.0
scikit-learn>=1.3.0
